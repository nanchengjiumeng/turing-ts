//图灵插件文件添加到工程的“rc”资源目录中

变量 Ret = 是否注册("TURING.FISR")
如果(Ret)
    调试输出("插件已注册!")
否则
    注册插件(获取资源路径("rc:TURING.dll"),真)
结束
变量 TURING = 插件("TURING.FISR")
TURING.Pixel_FromScreen(72,999,135,1020)
TURING.Filter_Binaryzation("0-254")

